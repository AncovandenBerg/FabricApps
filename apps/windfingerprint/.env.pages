# Loaded only for `vite build --mode pages` (see `build:pages` in package.json).
# Builds a backend-less static showcase for GitHub Pages -- no secrets here.
VITE_SHOWCASE=true
VITE_BASE_PATH=/FabricApps/windfingerprint/
